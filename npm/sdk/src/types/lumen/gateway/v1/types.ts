// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: lumen/gateway/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "lumen.gateway.v1";

export enum ContractStatus {
  CONTRACT_STATUS_UNSPECIFIED = 0,
  CONTRACT_STATUS_PENDING = 1,
  CONTRACT_STATUS_ACTIVE = 2,
  CONTRACT_STATUS_CANCELED = 3,
  CONTRACT_STATUS_COMPLETED = 4,
  CONTRACT_STATUS_FINALIZED = 5,
  UNRECOGNIZED = -1,
}

export function contractStatusFromJSON(object: any): ContractStatus {
  switch (object) {
    case 0:
    case "CONTRACT_STATUS_UNSPECIFIED":
      return ContractStatus.CONTRACT_STATUS_UNSPECIFIED;
    case 1:
    case "CONTRACT_STATUS_PENDING":
      return ContractStatus.CONTRACT_STATUS_PENDING;
    case 2:
    case "CONTRACT_STATUS_ACTIVE":
      return ContractStatus.CONTRACT_STATUS_ACTIVE;
    case 3:
    case "CONTRACT_STATUS_CANCELED":
      return ContractStatus.CONTRACT_STATUS_CANCELED;
    case 4:
    case "CONTRACT_STATUS_COMPLETED":
      return ContractStatus.CONTRACT_STATUS_COMPLETED;
    case 5:
    case "CONTRACT_STATUS_FINALIZED":
      return ContractStatus.CONTRACT_STATUS_FINALIZED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ContractStatus.UNRECOGNIZED;
  }
}

export function contractStatusToJSON(object: ContractStatus): string {
  switch (object) {
    case ContractStatus.CONTRACT_STATUS_UNSPECIFIED:
      return "CONTRACT_STATUS_UNSPECIFIED";
    case ContractStatus.CONTRACT_STATUS_PENDING:
      return "CONTRACT_STATUS_PENDING";
    case ContractStatus.CONTRACT_STATUS_ACTIVE:
      return "CONTRACT_STATUS_ACTIVE";
    case ContractStatus.CONTRACT_STATUS_CANCELED:
      return "CONTRACT_STATUS_CANCELED";
    case ContractStatus.CONTRACT_STATUS_COMPLETED:
      return "CONTRACT_STATUS_COMPLETED";
    case ContractStatus.CONTRACT_STATUS_FINALIZED:
      return "CONTRACT_STATUS_FINALIZED";
    case ContractStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Gateway {
  id: number;
  /** signer address */
  operator: string;
  /** distribution address */
  payout: string;
  active: boolean;
  metadata: string;
  /** unix seconds */
  createdAt: number;
  activeClients: number;
  cancellations: number;
}

export interface Contract {
  id: number;
  /** cosmos address of customer */
  client: string;
  gatewayId: number;
  priceUlmn: number;
  storageGbPerMonth: number;
  networkGbPerMonth: number;
  monthsTotal: number;
  /** unix seconds */
  startTime: number;
  /** sdk.Int encoded */
  escrowUlmn: string;
  claimedMonths: number;
  status: ContractStatus;
  metadata: string;
  /** unix seconds */
  nextPayoutTime: number;
}

function createBaseGateway(): Gateway {
  return {
    id: 0,
    operator: "",
    payout: "",
    active: false,
    metadata: "",
    createdAt: 0,
    activeClients: 0,
    cancellations: 0,
  };
}

export const Gateway: MessageFns<Gateway> = {
  encode(message: Gateway, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.operator !== "") {
      writer.uint32(18).string(message.operator);
    }
    if (message.payout !== "") {
      writer.uint32(26).string(message.payout);
    }
    if (message.active !== false) {
      writer.uint32(32).bool(message.active);
    }
    if (message.metadata !== "") {
      writer.uint32(42).string(message.metadata);
    }
    if (message.createdAt !== 0) {
      writer.uint32(48).uint64(message.createdAt);
    }
    if (message.activeClients !== 0) {
      writer.uint32(56).uint32(message.activeClients);
    }
    if (message.cancellations !== 0) {
      writer.uint32(64).uint32(message.cancellations);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Gateway {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGateway();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operator = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payout = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.metadata = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.createdAt = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.activeClients = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.cancellations = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Gateway {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      operator: isSet(object.operator) ? globalThis.String(object.operator) : "",
      payout: isSet(object.payout) ? globalThis.String(object.payout) : "",
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
      activeClients: isSet(object.activeClients) ? globalThis.Number(object.activeClients) : 0,
      cancellations: isSet(object.cancellations) ? globalThis.Number(object.cancellations) : 0,
    };
  },

  toJSON(message: Gateway): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.operator !== "") {
      obj.operator = message.operator;
    }
    if (message.payout !== "") {
      obj.payout = message.payout;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.activeClients !== 0) {
      obj.activeClients = Math.round(message.activeClients);
    }
    if (message.cancellations !== 0) {
      obj.cancellations = Math.round(message.cancellations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Gateway>, I>>(base?: I): Gateway {
    return Gateway.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Gateway>, I>>(object: I): Gateway {
    const message = createBaseGateway();
    message.id = object.id ?? 0;
    message.operator = object.operator ?? "";
    message.payout = object.payout ?? "";
    message.active = object.active ?? false;
    message.metadata = object.metadata ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.activeClients = object.activeClients ?? 0;
    message.cancellations = object.cancellations ?? 0;
    return message;
  },
};

function createBaseContract(): Contract {
  return {
    id: 0,
    client: "",
    gatewayId: 0,
    priceUlmn: 0,
    storageGbPerMonth: 0,
    networkGbPerMonth: 0,
    monthsTotal: 0,
    startTime: 0,
    escrowUlmn: "",
    claimedMonths: 0,
    status: 0,
    metadata: "",
    nextPayoutTime: 0,
  };
}

export const Contract: MessageFns<Contract> = {
  encode(message: Contract, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.client !== "") {
      writer.uint32(18).string(message.client);
    }
    if (message.gatewayId !== 0) {
      writer.uint32(24).uint64(message.gatewayId);
    }
    if (message.priceUlmn !== 0) {
      writer.uint32(32).uint64(message.priceUlmn);
    }
    if (message.storageGbPerMonth !== 0) {
      writer.uint32(40).uint64(message.storageGbPerMonth);
    }
    if (message.networkGbPerMonth !== 0) {
      writer.uint32(48).uint64(message.networkGbPerMonth);
    }
    if (message.monthsTotal !== 0) {
      writer.uint32(56).uint32(message.monthsTotal);
    }
    if (message.startTime !== 0) {
      writer.uint32(64).uint64(message.startTime);
    }
    if (message.escrowUlmn !== "") {
      writer.uint32(74).string(message.escrowUlmn);
    }
    if (message.claimedMonths !== 0) {
      writer.uint32(80).uint32(message.claimedMonths);
    }
    if (message.status !== 0) {
      writer.uint32(88).int32(message.status);
    }
    if (message.metadata !== "") {
      writer.uint32(98).string(message.metadata);
    }
    if (message.nextPayoutTime !== 0) {
      writer.uint32(104).uint64(message.nextPayoutTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Contract {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContract();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.client = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.gatewayId = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.priceUlmn = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.storageGbPerMonth = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.networkGbPerMonth = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.monthsTotal = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.startTime = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.escrowUlmn = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.claimedMonths = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.metadata = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.nextPayoutTime = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Contract {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      client: isSet(object.client) ? globalThis.String(object.client) : "",
      gatewayId: isSet(object.gatewayId) ? globalThis.Number(object.gatewayId) : 0,
      priceUlmn: isSet(object.priceUlmn) ? globalThis.Number(object.priceUlmn) : 0,
      storageGbPerMonth: isSet(object.storageGbPerMonth) ? globalThis.Number(object.storageGbPerMonth) : 0,
      networkGbPerMonth: isSet(object.networkGbPerMonth) ? globalThis.Number(object.networkGbPerMonth) : 0,
      monthsTotal: isSet(object.monthsTotal) ? globalThis.Number(object.monthsTotal) : 0,
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      escrowUlmn: isSet(object.escrowUlmn) ? globalThis.String(object.escrowUlmn) : "",
      claimedMonths: isSet(object.claimedMonths) ? globalThis.Number(object.claimedMonths) : 0,
      status: isSet(object.status) ? contractStatusFromJSON(object.status) : 0,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
      nextPayoutTime: isSet(object.nextPayoutTime) ? globalThis.Number(object.nextPayoutTime) : 0,
    };
  },

  toJSON(message: Contract): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.client !== "") {
      obj.client = message.client;
    }
    if (message.gatewayId !== 0) {
      obj.gatewayId = Math.round(message.gatewayId);
    }
    if (message.priceUlmn !== 0) {
      obj.priceUlmn = Math.round(message.priceUlmn);
    }
    if (message.storageGbPerMonth !== 0) {
      obj.storageGbPerMonth = Math.round(message.storageGbPerMonth);
    }
    if (message.networkGbPerMonth !== 0) {
      obj.networkGbPerMonth = Math.round(message.networkGbPerMonth);
    }
    if (message.monthsTotal !== 0) {
      obj.monthsTotal = Math.round(message.monthsTotal);
    }
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.escrowUlmn !== "") {
      obj.escrowUlmn = message.escrowUlmn;
    }
    if (message.claimedMonths !== 0) {
      obj.claimedMonths = Math.round(message.claimedMonths);
    }
    if (message.status !== 0) {
      obj.status = contractStatusToJSON(message.status);
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    if (message.nextPayoutTime !== 0) {
      obj.nextPayoutTime = Math.round(message.nextPayoutTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Contract>, I>>(base?: I): Contract {
    return Contract.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Contract>, I>>(object: I): Contract {
    const message = createBaseContract();
    message.id = object.id ?? 0;
    message.client = object.client ?? "";
    message.gatewayId = object.gatewayId ?? 0;
    message.priceUlmn = object.priceUlmn ?? 0;
    message.storageGbPerMonth = object.storageGbPerMonth ?? 0;
    message.networkGbPerMonth = object.networkGbPerMonth ?? 0;
    message.monthsTotal = object.monthsTotal ?? 0;
    message.startTime = object.startTime ?? 0;
    message.escrowUlmn = object.escrowUlmn ?? "";
    message.claimedMonths = object.claimedMonths ?? 0;
    message.status = object.status ?? 0;
    message.metadata = object.metadata ?? "";
    message.nextPayoutTime = object.nextPayoutTime ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
